<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
</head>
<body>
    {{> nav}}
    
    <h1>{{message}}</h1>

    <div>
        <ul>
            {{#each menus}}
                <h2>{{this.name}}</h2>
                <p>{{this.description}}</p>
            {{/each}}
    </div>
</body>

</html>